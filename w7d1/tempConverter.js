const round = (value) => Math.round(value * 10) / 10


const tempConverter = (value, cToF) => {
  if (cToF) {
    // weird math for c to f
  } else {
    // weird math for f to c
  }

  return round(calculatedValue)
}


module.exports = tempConverter

// npm run question 0