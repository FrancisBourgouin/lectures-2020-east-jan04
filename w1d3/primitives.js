// What are primitives?

// Boolean
const IsItTrue = true;

// String
const ImAString = "violin"

// Number
const meaningOfLife = 42

// Null

const wantToReturnNothing = null

// Undefined

const IHaveNothingToReturn = undefined

// Symbol

const addTwoNumbers = function (a, b) {
  if (Number.isNaN(Number(a)) || Number.isNaN(Number(b))) {
    return null
  }
}

let variableToChange

if (true) {
  variableToChange = 5
}

console.log(variableToChange)