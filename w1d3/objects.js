// Object methods

const superCoolObject = {
  ice: "pretty cold",
  snow: "not that cold",
  iceCream: "very cool",
}

// access values 

// dot notation

// square bracket notation

// Modifying an element inside an object

// Accessing the property inside the object, then assigning it a new value
superCoolObject.iceCream = "very cold"

// Add elements to an object

// Accessing the property inside the object doesn't exist yet, then assigning it a new value
superCoolObject.vanillaIce = "Ice Ice Baby"

console.log(superCoolObject)